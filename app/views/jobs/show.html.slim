
markdown:
  # #{@job.title}

  #{@job.terms.titlecase} at **Company Name**

  *#{@job.location}* #{ "(Remote)" if @job.remote }

  *Created #{pluralize((@job.created_at.to_date..Date.today).count, 'day') + ' ago.'}*

hr

p
  = markdown @job.description


= link_to 'Edit', edit_job_path(@job)
